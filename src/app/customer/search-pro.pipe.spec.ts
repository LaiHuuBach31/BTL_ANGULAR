import { SearchProPipe } from './search-pro.pipe';

describe('SearchProPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchProPipe();
    expect(pipe).toBeTruthy();
  });
});
